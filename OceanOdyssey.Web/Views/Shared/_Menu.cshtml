<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

<nav class="navbar navbar-expand-lg bg-primary shadow">
    <div class="container">
        <a class="navbar-brand text-white fw-bold" href="@Url.Action("Index","Home")">
            <i class="fa-solid fa-ship"></i> OceanOdyssey
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">

                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold" asp-controller="Barco" asp-action="Index">
                        <i class="fa-solid fa-ship"></i> Barcos
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold" asp-controller="Habitacion" asp-action="Index">
                        <i class="bi bi-house-door"></i> Habitaciones
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold" asp-controller="Crucero" asp-action="Index">
                        <i class="bi bi-emoji-sunglasses"></i> Cruceros
                    </a>
                </li>
                @if (User.IsInRole("Cliente") == true)
                {
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" asp-controller="Usuario" asp-action="HistorialReservas" asp-route-id="@User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value">
                            <i class="bi bi-calendar-check-fill"></i> Mis Reservas
                        </a>
                    </li>
                }
                @if (User.IsInRole("Admin") == true)
                {
                  <li class="nav-item">
                    <a class="nav-link text-white fw-semibold" asp-controller="ResumenReservacion" asp-action="Index">
                        <i class="bi bi-calendar-check-fill"></i> Reservas
                    </a>
                  </li>
                }
                @if (User.IsInRole("Admin") == true)
                {
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" asp-controller="Usuario" asp-action="IndexAdmin">
                            <i class="bi bi-calendar-check-fill"></i> Usuarios
                        </a>
                    </li>
                }
               
                @if (User.IsInRole("Admin") == true)
                {
                  <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle text-white fw-semibold" href="#" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-wrench"></i> Mantenimientos
                    </a>
                    <ul class="dropdown-menu">
                        <li><a asp-controller="Barco" asp-action="IndexAdmin" class="dropdown-item">  <i class="fa-solid fa-ship"></i> Barcos</a></li>
                        
                        <li>
                            <a asp-controller="habitacion" asp-action="IndexAdmin" class="dropdown-item"><span class="d-none d-sm-inline"></span><i class="bi bi-house"></i> Habitaciones </a>
                        </li>
                        <li><a class="dropdown-item" href="@Url.Action("Create", "Crucero")">  <i class="bi bi-emoji-sunglasses"></i> Cruceros</a></li>

                   
                        
                    </ul>
                  </li>
                }
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <img class="rounded-circle mx-2 mt-1" src="~/images/profile.png" alt=@User.Identity!.Name width="32px" height="32px" />

                        <span class="d-none d-sm-inline mx-1">@User.Identity!.Name</span>

                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" asp-area="" asp-controller="Login" asp-action="LogOff">Salir</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
